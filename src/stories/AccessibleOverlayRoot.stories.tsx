import React from 'react';

import { AccessibleOverlayRoot } from '..';

export default {
  title: 'AccessibleOverlayRoot',
  component: AccessibleOverlayRoot,
};

export const Basic = () => <AccessibleOverlayRoot rootId="tota11y-root" />;
